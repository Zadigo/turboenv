envs:
  global:
    - name: somename
      value: what
      type: str
      condition: "if debug == True :: great / not"
  test:
    - name: somename
      value: what
      type: str

preset: django,redis,
